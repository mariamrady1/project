import 'package:flutter/material.dart';

class GGG extends StatelessWidget {
  const GGG({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Text('ggggg'),
    );
  }
}
